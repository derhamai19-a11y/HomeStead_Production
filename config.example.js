// Copy this file to config.js and fill in your Firebase project details.
// config.js is gitignored — your credentials stay local and out of version control.
//
// Steps:
//   1. Go to Firebase console → Project settings → Your apps → Web app
//   2. Copy each value below from the firebaseConfig snippet shown there
//   3. Save the file and reload the app — no rebuild needed
//
window.HOMESTEAD_CONFIG = {
  firebase: {
    apiKey: "REPLACE_ME",
    authDomain: "REPLACE_ME.firebaseapp.com",
    databaseURL: "https://REPLACE_ME-default-rtdb.firebaseio.com",
    projectId: "REPLACE_ME",
    storageBucket: "REPLACE_ME.firebasestorage.app",
    messagingSenderId: "REPLACE_ME",
    appId: "REPLACE_ME"
  }
};
